// const path = require('path');
//  const { createCanvas, loadImage } = require('canvas');

class OpenApiMod {
  async widget (options) {
    /*
    const canvas = createCanvas(1010, 480);
    const ctx = canvas.getContext('2d');
    const vertex = await loadImage(path.join(__dirname, '../static/assets/images/logo.svg'));

    ctx.font = '32px consolas';
    ctx.fillStyle = 'black';

    ctx.drawImage(vertex, 1010 - 96, 480 - 96, 64, 64);
    return canvas;
    */
  }
}

module.exports = OpenApiMod;
